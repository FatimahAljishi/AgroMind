1. cd backend
2. create a .env file and write: OPENAI_API_KEY=sk-proj-.... (API key)
3. create a virtual environment: python -m venv .venv
4. activate the virtual envrionment: source .venv/Scripts/activate
5. pip install -r requirements.txt
